---
title: "Extract methods from literal-array .each/class_eval codegen"
status: draft
updated: 2026-08-03
rfc: "0025-fidelity-verification-tooling"
cluster: null
deps: []
deps-rfc: []
est-loc: null
priority: null
pr: null
claim: null
assignee: null
blocked-by: null
closed-reason: null
---

## Context

`scripts/api-compare/extract-ruby-api.rb`'s `process_each_codegen` recognizes
`CONST.each do |x| … class_eval "def #{x}…" end` (relation/query_methods.rb's
`VALUE_METHODS` loop) but not the literal-array form,
`%w(year mon month day mday wday yday hour min sec usec nsec to_date).each do |method_name| … class_eval`
at `vendor/rails/activesupport/lib/active_support/time_with_zone.rb:440-448`.

Every one of those readers therefore has no Ruby counterpart in the manifest:
`pnpm parity:api:extra --package activesupport` lists `min`, `sec`, `wday`, `yday`,
`msec`, `mon`, `mday` among `time-with-zone.ts`'s 17 novel names even though
each is a real `TimeWithZone` method, and `parity:api` scores the file short by
the same members.

## Acceptance criteria

- The extractor records methods generated by a literal-array receiver in the
  `.each … class_eval` codegen path, the way it already does for a constant
  receiver.
- `TimeWithZone`'s delegated readers stop showing up in
  `pnpm parity:api:extra --package activesupport`, and the activesupport `parity:api`
  method count moves up accordingly.
