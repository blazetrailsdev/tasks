node_modules/
.DS_Store
.claude/settings.local.json
# Harness-generated restart prompts; one was committed by accident (see git history).
.claude-restart-prompt*
# Generated by scripts/build-index.mjs and rebuilt on demand by the CLI's
# loadIndex() (stale/missing → rebuild). Machine caches with no consumer that
# can't regenerate them; committing them only caused concurrent-mutation merge
# conflicts. index.md (the human registry) stays tracked.
index.json
search.json
